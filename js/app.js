const departure=new Date('2027-04-24T00:00:00+09:00');const days=document.querySelector('#days');function tick(){days.textContent=Math.max(0,Math.ceil((departure-Date.now())/86400000)).toLocaleString('ja-JP')}tick();setInterval(tick,3600000);const header=document.querySelector('.header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>20),{passive:true});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));